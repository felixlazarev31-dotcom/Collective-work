<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ToDo Список дел</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body class="todo-page dark-theme">
    <!-- Кнопка переключения темы -->
    <button class="theme-toggle" onclick="toggleTheme()">🌓 Сменить тему</button>

    <div class="todo-container">
        <div class="todo-header">
            <h2>Привет, <span>{{ username }}</span>! 👋</h2>
            <a href="/logout" class="btn-logout">Выйти из аккаунта</a>
        </div>

        <!-- Форма добавления -->
        <form action="/add" method="POST" class="add-form">
            <input type="text" name="title" placeholder="Что нужно сделать?" required>
            <select name="category">
                <option value="Общее">📁 Общее</option>
                <option value="Учеба">📚 Учеба</option>
                <option value="Работа">💼 Работа</option>
                <option value="Личное">🏠 Личное</option>
            </select>
            <input type="date" name="due_date">
            <button type="submit" class="btn-add">➕</button>
        </form>

        <!-- Список задач -->
        <div class="todo-list">
            {% for todo in todo_list %}
            <div class="todo-item">
                <div class="todo-info">
                    <span class="category">{{ todo.category }}</span>
                    <span class="title {% if todo.complete %}done{% endif %}">{{ todo.title }}</span>
                    {% if todo.due_date %}
                        <span class="due-date">📅 До: {{ todo.due_date.strftime('%d.%m.%Y') }}</span>
                    {% endif %}
                </div>
                <div class="todo-actions">
                    <a href="/complete/{{ todo.id }}" class="btn-complete {% if todo.complete %}undo{% endif %}">
                        {% if todo.complete %}↩ Вернуть{% else %}✓ Ок{% endif %}
                    </a>
                    <a href="/delete/{{ todo.id }}" class="btn-delete">🗑 Удалить</a>
                </div>
            </div>
            {% else %}
            <div class="empty-message">Задач пока нет. Добавьте первую! 🚀</div>
            {% endfor %}
        </div>
    </div>

    <script>
        // Функция переключения темы
        function toggleTheme() {
            document.body.classList.toggle('dark-theme');
            // Сохраняем выбор в localStorage
            if (document.body.classList.contains('dark-theme')) {
                localStorage.setItem('theme', 'dark');
            } else {
                localStorage.setItem('theme', 'light');
            }
        }

        // Загрузка сохраненной темы
        window.onload = function() {
            const savedTheme = localStorage.getItem('theme');
            if (savedTheme === 'dark') {
                document.body.classList.add('dark-theme');
            }
        }
    </script>
</body>
</html>
